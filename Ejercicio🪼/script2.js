const array = ['🍕','🍕','🍍','🍕','🍕'];


const existePiña = array.includes('🍍');

arr.forEach((n,index)=>{ 
    if(index>=start){
        arr[index]="🍍";
    }
    console.log(arr[index]);
});
